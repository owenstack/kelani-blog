---
import { cn } from "@/lib/utils";

const { className, hideMark = true } = Astro.props;
---

		<span class={cn("font-[logo] text-nowrap font-semibold", className)}>
			<a href={"/"}>Beyond Dogma {hideMark ? null : <sup>&reg;</sup>}</a>
		</span>
