import Form from "next/form";
import { Input } from "./ui/input";
import { Submit } from "./submit";

export function Comment() {
	const comment = async (form: FormData) => {
		try {
		} catch (error) {}
	};
	return <div>Something</div>;
}
